# T certificates

This directory is reserved for exact GAP certificates belonging to the
tetrahedral `T` case.  No `T` certificate is included in the present upload.
Future certificates should be placed in topic-specific subdirectories here.
